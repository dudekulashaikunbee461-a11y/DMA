# DMA
**DMA (Direct Memory Access):** DMA allows devices to transfer data directly to or from memory without continuous CPU involvement. It improves data transfer speed and reduces the workload on the CPU.
